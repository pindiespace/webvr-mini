import LoadPool from  './load-pool';

export default class LoadAudio extends LoadPool {

    /**
     * Base loader class.
     */

    constructor ( config ) {

        super( config );

    }

    init () {

    }


}