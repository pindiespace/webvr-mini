import LoadPool from  './load-pool';

export default class LoadVideo extends LoadPool {

    /**
     * Base loader class.
     */

    constructor ( config ) {

        super( config );

    }

    init () {

    }


}