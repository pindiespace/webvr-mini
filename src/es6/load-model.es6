import LoadPool from  './load-pool';

export default class LoadModel extends LoadPool {

    /**
     * Base loader class.
     */

    constructor ( config ) {

        super( config );

    }

    init () {

    }


}